import { CATEGORIES, PRIORITIES, STATUSES } from "@/lib/types";
import { titleCase } from "@/lib/utils";
export function TicketFilters({customer=false,agents=[]}:{customer?:boolean;agents?:{id:string;name:string}[]}){ return <form className="mb-5 grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-6"><input name="q" placeholder="Search ticket title…" className="h-10 rounded-xl border border-slate-300 px-3 text-sm md:col-span-2"/>{!customer&&<select name="view" className="h-10 rounded-xl border border-slate-300 px-3 text-sm"><option value="all">All tickets</option><option value="mine">My tickets</option><option value="unassigned">Unassigned</option></select>}<select name="status" className="h-10 rounded-xl border border-slate-300 px-3 text-sm"><option value="">All statuses</option>{STATUSES.map(x=><option key={x} value={x}>{titleCase(x)}</option>)}</select>{!customer&&<><select name="priority" className="h-10 rounded-xl border border-slate-300 px-3 text-sm"><option value="">All priorities</option>{PRIORITIES.map(x=><option key={x}>{titleCase(x)}</option>)}</select><select name="category" className="h-10 rounded-xl border border-slate-300 px-3 text-sm"><option value="">All categories</option>{CATEGORIES.map(x=><option key={x}>{titleCase(x)}</option>)}</select>{agents.length>0&&<select name="agent" className="h-10 rounded-xl border border-slate-300 px-3 text-sm"><option value="">Any agent</option>{agents.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select>}</>}<button className="h-10 rounded-xl bg-slate-900 px-4 text-sm font-semibold text-white">Apply</button></form>; }

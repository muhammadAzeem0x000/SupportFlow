import { Calendar,Tag,User } from "lucide-react"; import { CommentThread } from "@/components/comments/comment-thread"; import { AttachmentList } from "@/components/attachments/attachment-list"; import { PriorityBadge,SlaBadge,TicketStatusBadge } from "@/components/ui/badge"; import { TicketControls } from "@/components/tickets/ticket-controls"; import { TicketTimeline } from "@/components/tickets/ticket-timeline"; import type { CurrentMember,CommentRecord,TicketPriority,TicketStatus } from "@/lib/types"; import { formatDate,titleCase } from "@/lib/utils";
type Detail={id:string;title:string;description:string;category:string;priority:string;status:string;response_due_at:string;first_agent_response_at:string|null;created_at:string;assigned_agent_id:string|null;customer:{full_name:string}|null;agent:{full_name:string}|null;comments:unknown[];attachments:Array<{id:string;original_filename:string;size_bytes:number}>;events:unknown[]};
export function TicketDetail({ticket,member,agents}:{ticket:Detail;member:CurrentMember;agents:{id:string;name:string}[]}){return <><div className="mb-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col justify-between gap-5 lg:flex-row"><div><div className="mb-3 flex flex-wrap gap-2"><TicketStatusBadge status={ticket.status as TicketStatus}/><PriorityBadge priority={ticket.priority as TicketPriority}/><SlaBadge dueAt={ticket.response_due_at} responseAt={ticket.first_agent_response_at}/></div><h1 className="text-2xl font-bold tracking-tight">{ticket.title}</h1><div className="mt-3 flex flex-wrap gap-4 text-xs text-slate-500"><span className="flex items-center gap-1"><Tag className="h-3.5 w-3.5"/>{titleCase(ticket.category)}</span><span className="flex items-center gap-1"><User className="h-3.5 w-3.5"/>{ticket.customer?.full_name}</span><span className="flex items-center gap-1"><Calendar className="h-3.5 w-3.5"/>{formatDate(ticket.created_at)}</span></div></div><TicketControls ticketId={ticket.id} role={member.role} status={ticket.status as TicketStatus} priority={ticket.priority as TicketPriority} assignedAgentId={ticket.assigned_agent_id} agents={agents}/></div><div className="mt-6 border-t border-slate-100 pt-6"><h2 className="mb-2 text-sm font-semibold">Description</h2><p className="whitespace-pre-wrap text-sm leading-7 text-slate-600">{ticket.description}</p></div></div><div className="grid gap-6 xl:grid-cols-[minmax(0,1.55fr)_minmax(20rem,.75fr)]"><CommentThread ticketId={ticket.id} initialComments={ticket.comments as CommentRecord[]} currentUserId={member.userId}/><div className="space-y-6"><AttachmentList ticketId={ticket.id} initial={ticket.attachments}/><TicketTimeline events={ticket.events as Parameters<typeof TicketTimeline>[0]["events"]}/></div></div></>}

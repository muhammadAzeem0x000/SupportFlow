import { Circle } from "lucide-react"; import { formatDate,titleCase } from "@/lib/utils";
export function TicketTimeline({events}:{events:Array<{id:string;event_type:string;old_value:string|null;new_value:string|null;created_at:string;actor?:{full_name:string}|null}>}){return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold">Ticket history</h2><div className="mt-5 space-y-5">{events.map(event=><div key={event.id} className="relative flex gap-3"><Circle className="mt-1 h-3 w-3 fill-indigo-500 text-indigo-500"/><div><p className="text-sm font-medium">{titleCase(event.event_type)}</p>{event.old_value&&<p className="text-xs text-slate-500">{titleCase(event.old_value)} → {event.new_value?titleCase(event.new_value):"None"}</p>}<p className="mt-1 text-xs text-slate-400">{event.actor?.full_name??"User"} · {formatDate(event.created_at)}</p></div></div>)}</div></section>}

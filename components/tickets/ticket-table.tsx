import Link from "next/link";
import { EmptyState } from "@/components/ui/empty-state";
import { PriorityBadge, SlaBadge, TicketStatusBadge } from "@/components/ui/badge";
import type { TicketSummary } from "@/lib/types";
import { formatDate } from "@/lib/utils";
export function TicketTable({tickets,basePath}:{tickets:TicketSummary[];basePath:string}){ if(!tickets.length)return <EmptyState title="No tickets found" description="Try changing the filters or create a new ticket."/>; return <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-full divide-y divide-slate-200"><thead className="bg-slate-50"><tr>{["Ticket","Customer","Status","Priority","Assigned","SLA","Created"].map(h=><th key={h} className="whitespace-nowrap px-5 py-3 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{tickets.map(ticket=><tr key={ticket.id} className="hover:bg-slate-50"><td className="max-w-xs px-5 py-4"><Link className="font-semibold text-slate-900 hover:text-indigo-600" href={`${basePath}/${ticket.id}`}>{ticket.title}</Link><p className="mt-1 text-xs capitalize text-slate-500">{ticket.category}</p></td><td className="whitespace-nowrap px-5 py-4 text-sm text-slate-600">{ticket.customer?.full_name??"—"}</td><td className="px-5 py-4"><TicketStatusBadge status={ticket.status}/></td><td className="px-5 py-4"><PriorityBadge priority={ticket.priority}/></td><td className="whitespace-nowrap px-5 py-4 text-sm text-slate-600">{ticket.agent?.full_name??"Unassigned"}</td><td className="whitespace-nowrap px-5 py-4"><SlaBadge dueAt={ticket.response_due_at} responseAt={ticket.first_agent_response_at}/></td><td className="whitespace-nowrap px-5 py-4 text-sm text-slate-500">{formatDate(ticket.created_at)}</td></tr>)}</tbody></table></div></div>; }

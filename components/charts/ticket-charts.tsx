"use client";
import { Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { titleCase } from "@/lib/utils";
export function TicketCharts({status,priority}:{status:Record<string,number>;priority:Record<string,number>}){ const statusData=Object.entries(status).map(([name,value])=>({name:titleCase(name),value})); const priorityData=Object.entries(priority).map(([name,value])=>({name:titleCase(name),value})); const colors=["#3157d5","#d97706","#059669","#64748b"]; return <div className="grid gap-4 lg:grid-cols-[1.45fr_.55fr]"><section className="border border-[#deded8] bg-white p-5"><p className="font-mono text-[10px] uppercase tracking-[0.14em] text-slate-400">Workload</p><h2 className="mt-1 font-semibold">Tickets by status</h2><div className="mt-5 h-64"><ResponsiveContainer width="100%" height="100%"><BarChart data={statusData}><CartesianGrid stroke="#e5e7eb" vertical={false}/><XAxis dataKey="name" tick={{fontSize:12}} axisLine={false} tickLine={false}/><YAxis allowDecimals={false} axisLine={false} tickLine={false}/><Tooltip/><Bar dataKey="value" fill="#3157d5" radius={[2,2,0,0]}/></BarChart></ResponsiveContainer></div></section><section className="border border-[#deded8] bg-white p-5"><p className="font-mono text-[10px] uppercase tracking-[0.14em] text-slate-400">Mix</p><h2 className="mt-1 font-semibold">Priority</h2><div className="mt-5 h-64"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={priorityData} dataKey="value" nameKey="name" innerRadius={58} outerRadius={82} paddingAngle={1}>{priorityData.map((entry,index)=><Cell key={entry.name} fill={colors[index%colors.length]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div></section></div>; }

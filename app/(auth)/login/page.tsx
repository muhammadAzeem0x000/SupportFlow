import { Headphones, ShieldCheck, Sparkles } from "lucide-react";
import { LoginForm } from "@/components/auth/login-form";
import { getCurrentMember } from "@/lib/auth/current-member";
import { redirect } from "next/navigation";

export default async function LoginPage(){ const member=await getCurrentMember(); if(member?.isActive) redirect(member.role==="customer"?"/customer/tickets":"/dashboard"); return <main className="grid min-h-screen lg:grid-cols-[1.05fr_.95fr]"><section className="relative hidden overflow-hidden bg-slate-950 p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="absolute -right-24 -top-24 h-96 w-96 rounded-full bg-indigo-600/30 blur-3xl"/><div className="relative flex items-center gap-3 font-bold"><span className="rounded-xl bg-indigo-500 p-2"><Headphones className="h-5 w-5"/></span>SupportFlow</div><div className="relative max-w-xl"><p className="mb-5 inline-flex items-center gap-2 rounded-full border border-indigo-400/30 bg-indigo-400/10 px-3 py-1 text-xs font-semibold text-indigo-200"><Sparkles className="h-3.5 w-3.5"/>Multi-tenant support, simplified</p><h1 className="text-5xl font-bold leading-tight tracking-tight">Resolve customer issues with clarity and confidence.</h1><p className="mt-6 text-lg leading-8 text-slate-300">A secure workspace for teams to triage tickets, collaborate in real time, and keep every customer in the loop.</p></div><div className="relative flex items-center gap-3 text-sm text-slate-400"><ShieldCheck className="h-5 w-5 text-emerald-400"/>Tenant-isolated by PostgreSQL Row Level Security</div></section><section className="flex items-center justify-center p-6 sm:p-12"><div className="w-full max-w-md"><div className="mb-8 lg:hidden"><div className="flex items-center gap-2 text-xl font-bold"><Headphones className="text-indigo-600"/>SupportFlow</div></div><p className="text-sm font-semibold text-indigo-600">Welcome back</p><h2 className="mt-2 text-3xl font-bold tracking-tight">Sign in to your workspace</h2><p className="mb-8 mt-3 text-sm text-slate-500">Use one of the seeded demo accounts below.</p><LoginForm/><div className="mt-8 rounded-2xl border border-slate-200 bg-white p-4 text-xs text-slate-600"><p className="font-semibold text-slate-900">Demo accounts · password SupportFlowDemo2026!</p><div className="mt-2 grid gap-1 sm:grid-cols-2"><span>admin@acme.demo</span><span>agent@acme.demo</span><span>customer@acme.demo</span><span>admin@globex.demo</span></div></div></div></section></main>; }

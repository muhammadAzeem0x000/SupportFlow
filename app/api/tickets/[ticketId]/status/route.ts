import { NextResponse } from "next/server"; import { requireApiMember } from "@/lib/auth/api-member"; import { AppError,errorResponse } from "@/lib/errors"; import { databaseError } from "@/lib/errors/database"; import { statusInputSchema } from "@/lib/validation/tickets";
export async function PATCH(request:Request,{params}:{params:Promise<{ticketId:string}>}){try{const {supabase}=await requireApiMember();const input=statusInputSchema.safeParse(await request.json());if(!input.success)throw new AppError("VALIDATION_ERROR","Choose a valid status.",422);const {ticketId}=await params;const {data,error}=await supabase.rpc("update_ticket_status",{p_ticket_id:ticketId,p_status:input.data.status});if(error)throw databaseError(error.message);return NextResponse.json({ticket:data});}catch(error){return errorResponse(error);}}

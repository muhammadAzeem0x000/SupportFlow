import { NextResponse } from "next/server"; import { requireApiMember } from "@/lib/auth/api-member"; import { AppError,errorResponse } from "@/lib/errors"; import { databaseError } from "@/lib/errors/database"; import { priorityInputSchema } from "@/lib/validation/tickets";
export async function PATCH(request:Request,{params}:{params:Promise<{ticketId:string}>}){try{const {supabase}=await requireApiMember(["admin"]);const input=priorityInputSchema.safeParse(await request.json());if(!input.success)throw new AppError("VALIDATION_ERROR","Choose a valid priority.",422);const {ticketId}=await params;const {data,error}=await supabase.rpc("update_ticket_priority",{p_ticket_id:ticketId,p_priority:input.data.priority});if(error)throw databaseError(error.message);return NextResponse.json({ticket:data});}catch(error){return errorResponse(error);}}
